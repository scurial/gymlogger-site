Public site for the GymLogger iPhone app: the privacy policy and support contact.
The source of truth for `privacy.md` is `docs/privacy.md` in the (private) app repo; it is copied here by `Tools/publish_privacy.sh`.
